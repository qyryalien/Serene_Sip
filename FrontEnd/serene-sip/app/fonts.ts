import { Playfair_Display, Work_Sans } from 'next/font/google';

export const PlayfairDisplay = Playfair_Display({ weight: ['400', '600'], subsets: ['latin'], display: 'swap', });
export const WorkSans = Work_Sans({ weight: ['400', '600'], subsets: ['latin'], display: 'swap', });